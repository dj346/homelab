apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: anibazarr-pvc
  namespace: bazarr
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 180Mi
  storageClassName: longhorn
  