apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: openbao-client-certificate
  namespace: openbao
spec:
  secretName: openbao-client-certificate-secret
  commonName: openbao-ops
  issuerRef:
    name: vault-issuer
    kind: ClusterIssuer