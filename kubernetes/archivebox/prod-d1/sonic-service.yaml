apiVersion: v1
kind: Service
metadata:
  name: sonic-service
  namespace: archivebox
spec:
  selector:
    app: sonic-service
  ports:
    - protocol: TCP
      port: 1491
      targetPort: 1491
  clusterIP: None # headless service for direct DNS resolution if needed