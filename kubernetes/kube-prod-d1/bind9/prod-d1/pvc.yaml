apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bind9-pvc
  namespace: bind9
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 0.5Gi
  storageClassName: longhorn
  