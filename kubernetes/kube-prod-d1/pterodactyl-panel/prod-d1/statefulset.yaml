apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: pterodactyl-panel
  namespace: pterodactyl-panel
spec:
  serviceName: pterodactyl-panel
  replicas: 1
  selector:
    matchLabels:
      app: pterodactyl-panel
  persistentVolumeClaimRetentionPolicy:
    whenDeleted: Retain
    whenScaled: Retain
  template:
    metadata:
      labels:
        app: pterodactyl-panel
    spec:
      initContainers:
        - name: restore-from-backup
          image: alpine@sha256:5b10f432ef3da1b8d4c7eb6c487f2f5a8f096bc91145e68878dd4a5019afde11
          command: ["sh", "-c"]
          args:
            - |
              if [ "$RESTORE_BACKUP" = "1" ]; then
                echo "Restoring backup from original PVC..."
                apk add --no-cache rsync
                rsync -avh --delete /backup/ /data/
              else
                echo "Skipping backup restore"
              fi
          env:
            - name: RESTORE_BACKUP
              value: "0" # Set this to "0" to skip restore
          volumeMounts:
            - name: pterodactyl-panel-config
              mountPath: /data
            - name: pterodactyl-backup-storage
              mountPath: /backup
      containers:
        - image: ghcr.io/pterodactyl/panel:latest@sha256:d9efe340f19ad4d9d62c9a52e6e05b231296875cbe30fec760172c590a4456c3
          imagePullPolicy: IfNotPresent
          name: pterodactyl-panel
          envFrom:
            - configMapRef:
                name: pterodactyl-panel
            - secretRef:
                name: pterodactyl-panel-secrets        
          ports:
            - containerPort: 80
              name: http
              protocol: TCP
          livenessProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - |
                  curl -sf http://localhost:80 || exit 1
            initialDelaySeconds: 5
            periodSeconds: 10
            #timeoutSeconds: 1
            successThreshold: 1
            failureThreshold: 10
          readinessProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - |
                  curl -sf http://localhost:80 || exit 1
            initialDelaySeconds: 5
            periodSeconds: 10
            #timeoutSeconds: 1
            successThreshold: 1
            failureThreshold: 3
          volumeMounts:
            - name: pterodactyl-panel-config
              mountPath: /app/var/
              subPath: var
            - name: pterodactyl-panel-config
              mountPath: /etc/nginx/http.d/
              subPath: http.d
            - name: pterodactyl-panel-config
              mountPath: /etc/letsencrypt/
              subPath: letsencrypt
            - name: pterodactyl-panel-config
              mountPath: /app/storage/logs
              subPath: logs
  volumeClaimTemplates:
    - metadata:
        name: pterodactyl-panel-config
      spec:
        accessModes: [ "ReadWriteOnce" ]
        storageClassName: longhorn
        resources:
          requests:
            storage: 1Gi
