apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mimir-minio-tenant
  namespace: argocd
  labels: 
    app.kubernetes.io/name: mimir-minio-tenant
    app.kubernetes.io/component: app
spec:
  project: default
  destination:
    server: https://kubernetes.default.svc
    namespace: mimir-minio-tenant
  sources:
    - repoURL: https://operator.min.io
      chart: tenant
      targetRevision: 7.1.1
      helm:
        valueFiles:
        - $values/kubernetes/mimir-minio-tenant/prod-d1/values.yaml
    - repoURL: 'https://gitea.kube-prod-d1.mclacken.net/dj346/homelab.git'
      targetRevision: HEAD
      path: kubernetes/mimir-minio-tenant/prod-d1
      ref: values
  syncPolicy:
    syncOptions:
      - CreateNamespace=true 
    automated: 
      prune: true 
      selfHeal: true 
      allowEmpty: false
    retry:
      limit: 0 # the number of retries to attempt
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m