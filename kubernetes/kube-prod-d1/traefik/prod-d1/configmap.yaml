apiVersion: v1
kind: ConfigMap
metadata:
  name: traefik-configmap
  namespace: traefik
data:
  config.yaml: |
    http:
      services:
        wings-pwings-prod-d1:
          loadBalancer:
              servers:
                - url: "http://10.15.21.150:443"
              passHostHeader: true
