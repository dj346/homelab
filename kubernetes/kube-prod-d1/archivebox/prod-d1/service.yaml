# ArchiveBox Service
apiVersion: v1
kind: Service
metadata:
  name: archivebox
  namespace: archivebox
spec:
  selector:
    app: archivebox
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      name: http
  type: ClusterIP