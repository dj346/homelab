apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jellyseer-pvc
  namespace: jellyseer
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: longhorn
  