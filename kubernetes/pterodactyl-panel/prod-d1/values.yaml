global:
  defaultStorageClass: longhorn

auth:
  database: panel
  username: pterodactyl
  existingSecret: pterodactyl-panel-secrets 

primary:
  extraFlags: "--default-authentication-plugin=mysql_native_password"