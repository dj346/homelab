apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: aniradarr
  name: aniradarr
  namespace: radarr
spec:
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: aniradarr
  template:
    metadata:
      labels:
        app: aniradarr
    spec:
      initContainers:
        - name: restore-from-backup
          image: alpine@sha256:5b10f432ef3da1b8d4c7eb6c487f2f5a8f096bc91145e68878dd4a5019afde11
          command: ["sh", "-c"]
          args:
            - |
              if [ "$RESTORE_BACKUP" = "1" ]; then
                echo "Restoring backup from original PVC..."
                apk add --no-cache rsync
                rsync -avh --delete /backup/ /data/
              else
                echo "Skipping backup restore"
              fi
          env:
            - name: RESTORE_BACKUP
              value: "1" # Set this to "0" to skip restore
          volumeMounts:
            - name: aniradarr-config
              mountPath: /data
            - name: aniradarr-backup-storage
              mountPath: /backup
      containers:
        - name: aniradarr
          image: lscr.io/linuxserver/radarr:latest@sha256:15417a594ebda4c660a9fa9748e7199d33e2d17b31bbc5ad7ba2e86f0b414763
          env:
          - name: PUID
            value: "1000"
          - name: PGID
            value: "1000"
          - name: TZ
            value: "America/Los_Angeles"
          ports:
          - name: http
            containerPort: 7878
          volumeMounts:
          - name: aniradarr-config
            mountPath: /config
          - name: data
            mountPath: /data
      volumes:
        - name: aniradarr-config
          persistentVolumeClaim:
            claimName: aniradarr-pvc
            readOnly: false
        - name: aniradarr-backup-storage ## Uncomment this and set claimname to PVC
          persistentVolumeClaim:
            claimName: pvc-6ec2a144-1601-4825-bf7d-265c5077ce8b
            readOnly: false
        - name: data
          nfs:
            server: 10.15.21.5
            path: /mnt/user/video/
        