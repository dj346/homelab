apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: aniradarr-pvc
  namespace: radarr
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: longhorn
  