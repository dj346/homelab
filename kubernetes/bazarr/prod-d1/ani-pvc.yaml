apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: anibazarr-pvc
  namespace: anibazarr
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: longhorn
  