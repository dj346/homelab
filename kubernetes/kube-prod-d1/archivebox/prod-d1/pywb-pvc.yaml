apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pywb-pvc
  namespace: archivebox
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: longhorn
  