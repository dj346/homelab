apiVersion: v1
kind: Service
metadata:
  name: anisonarr-web
  namespace: sonarr
  labels:
    app: anisonarr
spec:
  type: ClusterIP
  ports:
    - port: 8989
      targetPort: 8989
      protocol: TCP
  selector:
    app: anisonarr