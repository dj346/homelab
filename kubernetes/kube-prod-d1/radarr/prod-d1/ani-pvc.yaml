apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: aniradarr-pvc
  namespace: radarr
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 300Mi
  storageClassName: longhorn
  