apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: archivebox-pvc
  namespace: archivebox
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  storageClassName: longhorn
  