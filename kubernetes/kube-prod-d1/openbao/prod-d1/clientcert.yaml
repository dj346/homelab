apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: openbao-client
  namespace: openbao
spec:
  secretName: openbao-client-secret
  commonName: openbao-ops
  issuerRef:
    name: vault-issuer