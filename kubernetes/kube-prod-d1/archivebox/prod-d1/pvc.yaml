apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: archivebox-pvc
  namespace: archivebox
spec:
  accessModes:
    - ReadWriteOnce # This should be ReadWriteOnce.
  resources:
    requests:
      storage: 300Mi
  storageClassName: longhorn
  