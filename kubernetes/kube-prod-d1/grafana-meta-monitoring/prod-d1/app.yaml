apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: grafana-meta-monitoring
  namespace: argocd
  labels: 
    app.kubernetes.io/name: grafana-meta-monitoring
    app.kubernetes.io/component: app
spec:
  project: default
  destination:
    server: https://kubernetes.default.svc
    namespace: meta
  sources:
    - repoURL: https://grafana.github.io/helm-charts
      chart: meta-monitoring
      targetRevision: 1.3.0
      helm:
        valueFiles:
        - $values/kubernetes/grafana-meta-monitoring/prod-d1/values.yaml
    - repoURL: 'ssh://git@gitea-ssh.gitea.svc.cluster.local:2222/dj346/homelab.git'
      targetRevision: HEAD
      path: kubernetes/grafana-meta-monitoring/prod-d1
      ref: values
  syncPolicy:
    syncOptions:
      - CreateNamespace=true 
    automated: 
      prune: true 
      selfHeal: true 
      allowEmpty: false
    retry:
      limit: 0 # the number of retries to attempt
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m