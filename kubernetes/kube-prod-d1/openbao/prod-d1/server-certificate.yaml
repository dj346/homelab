apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: openbao-ingressroute-certificate
  namespace: openbao
spec:
  secretName: openbao-certificate-secret
  issuerRef:
    name: cloudflare-clusterissuer
    kind: ClusterIssuer
  dnsNames:
    - "*.openbao.kube-prod-d1i.mclacken.net"