name: composeflux-prod-d1

include:
  - ../../../templates/composeflux/compose.yml

services:
  composeflux:
    container_name: composeflux-prod-d1
    hostname: composeflux-prod-d1

    environment:
      GIT_REPO_URL: git@github.com:dj346/homelab.git
      GIT_BRANCH: main
      STACK_PATH: docker/servers/openbao-seal-prod-d1