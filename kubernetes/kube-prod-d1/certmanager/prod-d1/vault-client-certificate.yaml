apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: openbao-client-certificate
  namespace: cert-manager
spec:
  secretName: openbao-client-certificate-secret
  commonName: certmanager-client-certificate
  issuerRef:
    name: vault-issuer
    kind: ClusterIssuer