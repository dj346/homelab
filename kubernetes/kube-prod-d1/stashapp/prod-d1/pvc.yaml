apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: stashapp-pvc
  namespace: stashapp
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
  storageClassName: longhorn
  