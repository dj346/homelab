apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: es-pvc
  namespace: tubearchivist
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: longhorn