apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: vault-certificate
  namespace: traefik
spec:
  secretName: vault-certificate-secret
  issuerRef:
    name: vault-issuer
    kind: ClusterIssuer
  commonName: traefik-vault-ca