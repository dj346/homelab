apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: anisonarr-pvc
  namespace: sonarr
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 450Mi
  storageClassName: longhorn
  