apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gluetun-pvc
  namespace: qbittorrent
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: longhorn
  