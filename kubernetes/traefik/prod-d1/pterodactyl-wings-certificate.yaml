apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: pterodactyl-wings-certificate
  namespace: traefik
spec:
  secretName: pterodactyl-wings-certificate
  issuerRef:
    name: cloudflare-clusterissuer
    kind: ClusterIssuer
  dnsNames:
    - wings.pwings-prod-d1.mclacken.net