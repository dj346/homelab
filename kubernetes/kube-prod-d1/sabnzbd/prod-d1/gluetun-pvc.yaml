apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gluetun-pvc
  namespace: sabnzbd
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 80Mi
  storageClassName: longhorn
  