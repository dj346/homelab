apiVersion: v1
kind: Service
metadata:
  name: anibazarr-web
  namespace: bazarr
  labels:
    app: anibazarr
spec:
  type: ClusterIP
  ports:
    - port: 6767
      targetPort: 6767
      protocol: TCP
  selector:
    app: anibazarr