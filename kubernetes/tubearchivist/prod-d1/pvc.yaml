apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cache-pvc
  namespace: tubearchivist
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2.5Gi
  storageClassName: longhorn